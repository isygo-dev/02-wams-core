server:
  tomcat:
    max-swallow-size: -1
    max-http-form-post-size: -1
  #valid range 0–65535
  port: 40411

eureka:
  instance:
    prefer-ip-address: true
    #hostname: discovery-service
  client:
    serviceUrl:
      defaultZone: http://discovery-service:8061/eureka/

camel:
  springboot:
    routes-include-pattern: file:camel/*.xml
    xmlRoutes: file:camel/*.xml

logging:
  level:
    eu.isygoit: info
    org.apache.kafka: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] ${LOG_LEVEL_PATTERN:-%5p} %m%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%X{traceId:-},%X{spanId:-}] ${LOG_LEVEL_PATTERN:-%5p} %m%n"
  file: pms.log

springdoc:
  packagesToScan: eu.isygoit
  api-docs:
    path=/api-docs:
  swagger-ui:
    path=/swagger-ui-custom:
      html:
    operationsSorter=method:

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.cassandra.CassandraDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.cassandra.CassandraReactiveDataAutoConfiguration
      - org.springframework.boot.autoconfigure.cassandra.CassandraAutoConfiguration
  cassandra:
    contact-points: cassandra
    keyspace-name: isygoit
    port: 9042
    local-datacenter: datacenter1
    schema-action: CREATE_IF_NOT_EXISTS
    create-keyspace: true
    username: admin
    password: admin
  servlet:
    multipart:
      enabled: true
      max-file-size: 30MB
      max-request-size: 30MB
  hazelcast:
    config: classpath:cache/hazelcast.xml
  output:
    ansi:
      enabled: always
  kafka:
    topics:
      register-api-permission: to-ims-register-api-permission
      register-account: to-ims-register-account
      send-email: to-mms-send-email
    producer:
      value-serializer: org.apache.kafka.common.serialization.ByteArraySerializer
      key-serializer: org.apache.kafka.common.serialization.ByteArraySerializer
      bootstrap-servers: localhost:9092
    consumer:
      auto-offset-reset: earliest
      bootstrap-servers: localhost:9092
      group-id: group-id
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  datasource:
    continue-on-error: true
    initialization-mode: always
    mode: true
    url: jdbc:postgresql://postgres:5432/postgres?currentSchema=pms
    username: postgres
    password: root
    hikari:
      # Maximum number of connections that can be maintained in the pool.
      # Each application instance can use up to 10 PostgreSQL connections.
      maximum-pool-size: 5
      # Minimum number of idle connections kept ready in the pool.
      minimum-idle: 2
      # Maximum time to wait for a connection when the pool is exhausted.
      # 30000 ms = 30 seconds.
      connection-timeout: 30000
      # Maximum time an idle connection can remain in the pool before being removed.
      # 600000 ms = 10 minutes.
      idle-timeout: 600000
      # Maximum lifetime of a connection before Hikari retires it.
      # 1800000 ms = 30 minutes.
      max-lifetime: 1800000
  flyway:
    baseline-on-migrate: true
    baseline-version: 1.0.0
    check-locations: true
    default-schema: pms
    enabled: false
    group: true
    locations: classpath:migration/01-dev
    target: 1.0.231013.1
    schemas: pms
    table: schema_migration
  jpa:
    hibernate:
      ddl-auto: update #create #update #validate #create-drop
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyHbmImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      show-sql: true
    properties:
      org:
        hibernate:
          envers:
            audit_table_prefix: AUD_
            revision_type_field_name: REV_TYPE
            audit_table_suffix: ''
            store_data_at_delete: 'true'
            revision_field_name: REV_ID
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false

management:
  tracing:
    sampling:
      probability: 1.0

app:
  feign:
    shouldNotFilterKey: ++SHOULD_NOT_FILTER$
    api:
      internal-key: Bearer eyJleHAiOiJuZXZlciIsImtpZFZlcnNpb24iOiJ2LTNmZjAyNTJkLWU2OWUtNDhkZC1hMTljLWMxMmY3MGJhODMyMSIsImFsZyI6IlJTMjU2In0.eyJzdWIiOiJyb290QHN1cGVyLXRlbmFudCIsImlzcyI6ImttcyIsImlhdCI6MTc4NjcwODA1NiwiYXVkIjpbIktNUy4qIl0sImxvZy1hcHAiOiJpZGVudGl0eS1zZXJ2aWNlIiwic2VuZGVyLXVzZXIiOiJyb290Iiwic2VuZGVyLXRlbmFudCI6InN1cGVyLXRlbmFudCJ9.C_4AB2oFLx3LBW4OfIlxyhoEIy2PU09hpRuWFCHjelM9BgGn_6agp9PpgF-BqTQICPN9T39aCsGsFRiumaPq-25m4rMI-4rk6ly4gD4Tqu-nh0PuivLRnnHxFUl-Eg-ZXFsgCEbaCdvEXr3yGkcagUOA_QtZDfd0_SPyD6Z6TAXlq1Xj_5_Jof3mGJj1BbWhkPtbwXUrNE89iZ6ezx50Gf7FjTFbv5TPqOTz47tTfuyStOd2jijb4NhNj2BIvp940uY6ZF4gfQvJYhIUNmEDf632w19TJ-9arXDtIs18LexB2cJ1tcrCnfUXFumOMCDR-GSFpOzMfdqziKb2HYGl5Q
        
  email:
    async: true
    broker: kafka # kafka, rest
  upload:
    directory: /uploads/pms
  i18n:
    location: classpath:i18n/messages
  jwt:
    secretKey: JwTsEcReTkEy
    deactivateOldTokens: false
    signatureAlgorithm: HS256
    life-time-ms: 14400000
    storage-type: LOCAL